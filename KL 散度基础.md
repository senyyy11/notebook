# KL 散度基础

## 1. KL 散度解决什么问题

KL 散度（Kullback–Leibler Divergence）用于比较两个**概率分布**：

- $P$：参考分布，常被理解为真实分布、目标分布或数据分布；
- $Q$：用来近似 $P$ 的模型分布或候选分布。

它回答的问题是：

> 当数据实际来自 $P$，但我们使用 $Q$ 来描述、预测或编码这些数据时，平均会多付出多少信息代价？

因此，KL 散度比较的不是两个普通数值，也不是两个样本点之间的几何距离，而是两个完整概率分布的失配程度。

## 2. 定义与符号

对于离散随机变量，KL 散度定义为：

$$D_{\mathrm{KL}}(P\|Q)=\sum_x P(x)\log\frac{P(x)}{Q(x)}$$

也可以写成期望形式：

$$D_{\mathrm{KL}}(P\|Q)=\mathbb{E}_{x\sim P}\left[\log\frac{P(x)}{Q(x)}\right]$$

其中：

- $x$ 表示随机变量的一个可能结果；
- $P(x)$ 和 $Q(x)$ 分别是两个分布为结果 $x$ 分配的概率；
- $x\sim P$ 表示按照 $P$ 对各个结果加权；
- $\log \frac{P(x)}{Q(x)}$ 表示结果 $x$ 在两个分布下的对数概率差。

对于连续随机变量，求和改为积分：

$$D_{\mathrm{KL}}(P\|Q)=\int p(x)\log\frac{p(x)}{q(x)}\,dx$$

## 3. KL 散度如何计算

以一个只有“成功”和“失败”两种结果的问题为例。假设：

$$P=(0.8,0.2),\qquad Q=(0.5,0.5)$$

使用自然对数计算：

$$D_{\mathrm{KL}}(P\|Q)=0.8\ln\frac{0.8}{0.5}+0.2\ln\frac{0.2}{0.5}$$

代入数值：

$$D_{\mathrm{KL}}(P\|Q)=0.8\ln 1.6+0.2\ln 0.4\approx 0.1927$$

所以：

$$D_{\mathrm{KL}}(P\|Q)\approx 0.193\ \text{nats}$$

这个计算可以拆成三步理解：

1. 使用 $P(x)/Q(x)$ 比较两个分布对同一结果分配的概率；
2. 取对数，得到两个分布在该结果上的信息差；
3. 使用 $P(x)$ 加权，对所有可能结果的信息差求平均。

单个结果的贡献可以为负。例如上面的第二项满足：

$$0.2\ln\frac{0.2}{0.5}<0$$

但所有结果加总后的 KL 散度一定非负：

$$D_{\mathrm{KL}}(P\|Q)\geq 0$$

## 4. 为什么要取对数

“取对数把概率的乘法关系转换为加法关系”这句话需要分成两个层次理解。

### 4.1 对单个概率比：除法变成减法

对于一个结果 $x$：

$$\log\frac{P(x)}{Q(x)}=\log P(x)-\log Q(x)$$

因此，对单个结果而言，取对数直接完成的是：

> 把概率之比转换成对数概率之差。

### 4.2 对多个事件：乘法变成加法

假设 $x_1$ 和 $x_2$ 相互独立，那么联合概率为：

$$P(x_1,x_2)=P(x_1)P(x_2)$$

两个分布的联合概率之比可以写成：

$$\frac{P(x_1,x_2)}{Q(x_1,x_2)}=\frac{P(x_1)}{Q(x_1)}\frac{P(x_2)}{Q(x_2)}$$

取对数以后：

$$\log\frac{P(x_1,x_2)}{Q(x_1,x_2)}=\log\frac{P(x_1)}{Q(x_1)}+\log\frac{P(x_2)}{Q(x_2)}$$

所以，“乘法变加法”描述的是多个事件联合发生时，多个概率比原本相乘，取对数后，每个事件提供的信息贡献可以相加。

事件不独立时，也可以使用条件概率分解：

$$P(x_{1:n})=\prod_{i=1}^{n}P(x_i\mid x_{<i})$$

于是整个序列的对数概率比为：

$$\log\frac{P(x_{1:n})}{Q(x_{1:n})}=\sum_{i=1}^{n}\log\frac{P(x_i\mid x_{<i})}{Q(x_i\mid x_{<i})}$$

因此，更准确的说法是：

> 取对数把单个概率比的除法转换成对数概率之差；对于多个事件，它又把联合概率比的乘法转换成逐事件信息贡献的加法。

## 5. 对数为什么具有信息论含义

### 5.1 单个事件的信息量

信息论把事件 $x$ 的信息量定义为：

$$I_P(x)=-\log P(x)$$

这个定义符合两个直觉：

- 事件越罕见，发生后提供的信息越多；
- 相互独立的事件联合发生时，信息量应当能够相加。

对于两个独立事件：

$$I(x_1,x_2)=-\log[P(x_1)P(x_2)]=I(x_1)+I(x_2)$$

这就是对数在信息论中的核心作用：概率相乘，对应信息量相加。

### 5.2 对数概率比表示信息代价之差

同一个结果 $x$ 在两个分布下的信息量分别为：

$$I_P(x)=-\log P(x),\qquad I_Q(x)=-\log Q(x)$$

因此：

$$\log\frac{P(x)}{Q(x)}=I_Q(x)-I_P(x)$$

它表示：

> 对于结果 $x$，使用 $Q$ 代替 $P$ 来描述它时，增加了多少信息代价。

例如：

$$P(x)=0.5,\qquad Q(x)=0.125$$

使用以 $2$ 为底的对数：

$$I_P(x)=-\log_2 0.5=1\ \text{bit}$$

$$I_Q(x)=-\log_2 0.125=3\ \text{bits}$$

所以：

$$\log_2\frac{P(x)}{Q(x)}=3-1=2\ \text{bits}$$

这表示，对于结果 $x$，按照 $Q$ 描述它比按照 $P$ 描述它多付出了 $2$ bit 的信息代价。

### 5.3 对数概率比也表示证据强度

$\log[P(x)/Q(x)]$ 也称为对数似然比：

- 大于 $0$：结果 $x$ 更符合 $P$；
- 小于 $0$：结果 $x$ 更符合 $Q$；
- 等于 $0$：$P$ 和 $Q$ 为结果 $x$ 分配了相同概率，该结果不能区分两个分布。

单个结果的信息差可能为正或负。KL 散度则按照 $P$ 对这些信息差求平均：

$$D_{\mathrm{KL}}(P\|Q)=\mathbb{E}_{x\sim P}[I_Q(x)-I_P(x)]$$

因此，KL 散度表示的是平均额外信息代价，而不是某一个结果上的局部差异。

## 6. KL 数值应该如何理解

### 6.1 数值大小

- $D_{\mathrm{KL}}(P\|Q)=0$：两个分布几乎处处相同；
- KL 较小：$Q$ 对 $P$ 的近似较好；
- KL 较大：$Q$ 与 $P$ 的失配较明显；
- KL 没有统一上界。

如果存在某个结果满足：

$$P(x)>0,\qquad Q(x)=0$$

那么 $P$ 认为可能发生的事件被 $Q$ 判断为绝不可能发生，此时：

$$D_{\mathrm{KL}}(P\|Q)=+\infty$$

### 6.2 数值单位

- 使用自然对数 $\ln$，单位是 nat；
- 使用以 $2$ 为底的对数 $\log_2$，单位是 bit。

只有在计算方向、对数底数、维度及求和或平均方式一致时，两个 KL 数值才适合直接比较。

## 7. KL 散度为什么不是真正的距离

KL 散度一般不满足对称性：

$$D_{\mathrm{KL}}(P\|Q)\neq D_{\mathrm{KL}}(Q\|P)$$

$D_{\mathrm{KL}}(P\|Q)$ 按照 $P$ 加权，关注的是 $P$ 认为重要的区域中，$Q$ 是否给出了足够概率；反过来，$D_{\mathrm{KL}}(Q\|P)$ 按照 $Q$ 加权，观察视角已经改变。

因此，KL 散度虽然经常被口语化地称为“分布之间的距离”，但它不是满足严格数学距离定义的度量。

## 8. 与熵和交叉熵的关系

分布 $P$ 的熵为：

$$H(P)=-\sum_x P(x)\log P(x)$$

它表示数据本身的平均不确定性。

使用 $Q$ 描述来自 $P$ 的数据时，交叉熵为：

$$H(P,Q)=-\sum_x P(x)\log Q(x)$$

三者满足：

$$D_{\mathrm{KL}}(P\|Q)=H(P,Q)-H(P)$$

也就是：

$$H(P,Q)=H(P)+D_{\mathrm{KL}}(P\|Q)$$

可以将它理解为：

> 交叉熵 = 数据本身固有的不确定性 + 模型分布失配造成的额外信息代价。

在监督学习中，数据分布 $P$ 通常固定，因此 $H(P)$ 对模型参数而言是常数。最小化交叉熵，也就等价于最小化相应的 KL 散度。

## 9. 最终总结

KL 散度最核心的含义是：

$$D_{\mathrm{KL}}(P\|Q)=\text{数据来自 }P\text{ 时，用 }Q\text{ 代替 }P\text{ 所产生的平均额外信息代价}$$

需要记住以下几点：

1. KL 散度比较的是两个概率分布，而不是两个样本点；
2. 第一个分布 $P$ 决定了加权视角，因此 KL 散度具有方向性；
3. 对单个概率比取对数，是把除法变成减法；对多个事件，则把联合概率比的乘法变成加法；
4. $-\log P(x)$ 表示事件的信息量，$\log[P(x)/Q(x)]$ 表示两个分布描述同一事件时的信息代价之差；
5. KL 散度是对这种信息代价之差按照 $P$ 求平均；
6. KL 散度非负，但不对称、没有统一上界，因此不是真正的数学距离。
