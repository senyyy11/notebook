web前端/应用端：用于接受用户对话，下达指令，输入图片数据，可在该部分进行数据标注，任务返回结果呈现
agent:理解用户需求，调度工具，这里使用一个通用能力较强的大模型

Qwen3-VL 这样的通用视觉大模型，直接部署到本地，启动推理，由agent调用，不替代专项模型，只充当通用的视觉专家，例如面对用户提出的较为宽泛的问题，专项模型中没用经过训练，这时候调用通用模型来进行检测，用于开放任务

训练平台部分采用YOLO进行，加载官方预训练好的YOLO模型，导入自己的图片数据，进行fine-tuning
封装训练流程，上传数据 → 标注 → 点击训练 → 等待 → 新模型上线
训练平台自动执行数据检查->数据增强->选择基础模型->训练->测试->生成

模型仓库，需要记录训练好后的一个模型的相关信息，包括模型名称，版本，基础模型，数yong据集，训练时间，mAP指标，状态，权重文件，使用MLflow进行相关记录
模型权重存储使用MinIO

完整流程：
用户告诉Agent：

> 我需要增加一个道路裂缝识别能力。

Agent查询：
      Capability Registry
发现
    road_crack_detection

    NOT FOUND

Agent回答：

```
当前系统不存在道路裂缝专项检测能力，
可以创建新的视觉能力。
```

用户选择：

```
名称：
道路裂缝

任务：
目标检测 / 分割
```

用户上传图片数据
上传到：

```
MinIO

datasets/
    road_crack/
        raw/
```

数据库：

```
dataset_id = 37
name = road_crack
status = labeling
```

进入Label Studio
用户完成：

```
裂缝Mask
```

或者：

```
无裂缝
有裂缝
```

具体取决于任务。

数据审核完成：

```
road_crack_dataset_v1
```

冻结数据集。（训练完成后需要知道模型是用那批数据训练出来的）

